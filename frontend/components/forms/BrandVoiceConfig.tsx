import { Card } from "@frontend/components/ui/Card";

export function BrandVoiceConfig() {
  return (
    <Card>
      <h2 className="mb-2 text-lg font-semibold">Brand Voice</h2>
      <p className="text-sm text-zinc-400">
        Tune tone, style, and vocabulary so every generated asset stays consistent across channels.
      </p>
    </Card>
  );
}
